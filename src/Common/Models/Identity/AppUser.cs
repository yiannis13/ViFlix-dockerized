using Microsoft.AspNetCore.Identity;

namespace Common.Models.Identity
{
    public class AppUser : IdentityUser
    {
        // if you want to add a column to the "Users" table, add here a new property
    }
}